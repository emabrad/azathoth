[tool.black]
line-length = 120
target-version = ['py36']
include = '\.pyi?$'
exclude = '''

(
  /(
    | \.git          # exclude some directories
    | \.venv
  )/
  | foo.py           # also separately exclude a file named foo.py in
                     # the root of the project
)
'''
